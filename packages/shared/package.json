{
  "name": "@event-mingle/shared",
  "version": "0.1.0",
  "private": true,
  "main": "./dist/index.js",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsup",
    "clean": "rimraf .turbo node_modules dist",
    "dev": "tsup --watch",
    "format": "prettier --check . --ignore-path ../../.gitignore",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@event-mingle/types": "*",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/node": "^20",
    "eslint": "^8",
    "rimraf": "^5.0.5",
    "tsup": "^8.0.2",
    "typescript": "^5"
  }
}
